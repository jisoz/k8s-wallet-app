apiVersion: v1
kind: ConfigMap
metadata:
  name: wallet-config
data:
  config.yaml: |
    database:
      host: postgres
      port: 5432
      user: postgres
      name: wallet
    redis:
      host: redis
      port: 6379

